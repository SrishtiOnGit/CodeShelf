import "./cta.css";

const CTA = () => {
  return (
    <div className="cta" data-aos="fade-up">
      <div className="cta-conatiner">
        <h1 className="cta-title">Ready to build smarter?</h1>
        <h1 className="cta-subtitle">
          Store your snippets once. Find them instantly. Reuse them forever.
        </h1>
        <button className="cta-btn">Start For Free</button>
      </div>
    </div>
  );
};

export default CTA;
